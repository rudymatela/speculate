_ :: Name  (holes: Name)
_ :: Prop  (holes: Prop)
_ :: Bool  (holes: Bool)
False :: Bool
True :: Bool
(==) :: Prop -> Prop -> Bool
eval :: Prop -> Prop
varOf :: Prop -> Name
subst :: Name -> Bool -> Prop -> Prop
taut :: Prop -> Bool

     (p == p) == True
eval (eval p) == eval p
taut (eval p) == taut p
     (p == q) == (q == p)

    p ==> True
False ==> p

     p ==>         subst n True q == subst n p q
     p ==>  eval (subst n True q) == eval (subst n p q)
     p ==>  subst n True (eval q) == subst n p (eval q)
taut p ==>     subst o (taut q) p == subst n False p
taut p ==>     subst o (taut q) p == subst n True p
taut p ==>     subst n (taut p) p == subst n True p
taut p ==>  eval (subst n True p) == eval (subst n False p)
taut q ==>     subst n (taut q) p == subst n True p
taut q ==>     subst o (taut p) q == subst n (taut p) q
taut q ==>     subst o (taut q) q == subst n (taut p) q
taut q ==> eval (subst n False q) == eval (subst n p q)
taut q ==>  eval (subst n True q) == eval (subst n p q)

